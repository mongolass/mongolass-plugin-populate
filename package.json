{
  "name": "mongolass-plugin-populate",
  "version": "0.4.0",
  "description": "[mongolass](https://github.com/mongolass/mongolass) `.populate` plugin",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha test.js"
  },
  "keywords": [
    "mongodb",
    "mongolass"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=4"
  },
  "dependencies": {
    "lodash": "4.17.10"
  },
  "devDependencies": {
    "co-mocha": "^1.2.2",
    "mocha": "^5.2.0",
    "mongolass": "*"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mongolass/mongolass-plugin-populate"
  }
}
